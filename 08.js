function findDifference(a, b) {
  
    var sum = a[0]*a[1]*a[2]-b[0]*b[1]*b[2];
    
    if (sum>0) {
      
      return sum;
      
    } else 
      
      return -sum;
    
    
    //loading...
  }